#include "monty.h"
/**
 * my_stack - prints the top element of the stack
 * @head: pointer to the head of the stack
 * @line_number: line number where the opcode occurs
 * Return: Nothing
*/
void my_stack(stack_t **head, unsigned int line_number)
{
	(void)head;
	(void)line_number;
	global_var.buff_index = 0;
}
